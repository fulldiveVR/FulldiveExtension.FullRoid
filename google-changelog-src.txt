What's New:

• Physical Controllers as Player 2: Optionally connect physical controllers starting from Player 2 while Player 1 stays on the on-screen controller — the virtual pad no longer disappears when a gamepad is plugged in
